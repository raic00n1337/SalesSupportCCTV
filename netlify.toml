# Netlify Configuration für Video-System-Konfigurator
# Next.js 14 App with API Routes

[build]
  # Build Command
  command = "npm run build"
  
  # Publish Directory - Next.js with API routes (not static export)
  publish = ".next"
  
  # Node Version
  environment = { NODE_VERSION = "20" }

# Enable Next.js Runtime for API routes
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Redirects für 404-Seite
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

# Headers für bessere Performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache-Control für statische Assets
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Development Context
[context.deploy-preview]
  command = "npm run build"

[context.branch-deploy]
  command = "npm run build"

# Production Context
[context.production]
  command = "npm run build"

