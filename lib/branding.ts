// Während der internen Testphase soll das Tool anonym nutzbar sein, damit
// Vertriebler es freiwillig ausprobieren können, ohne dass es als offizielles
// Securitas Technology Werkzeug erkennbar ist. Dafür wird das Logo sowie der
// Firmenname in UI, Browser-Tab und PDF-Export ausgeblendet.
//
// Für den späteren Live-Rollout einfach wieder auf `true` setzen.
export const BRANDING_ENABLED = false
